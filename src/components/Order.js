/**
 * An order on the order page
 * @param {string} id - the id of the order
 * @param {integer} amount - how much the order was
 * @param {integer} amountShipping - how many items are being shipped
 * @param {array} items - the actual items in the order
 * @param {integer} timestamp - when the order was placed
 * @param {array} images - images of the various products bought
 * @returns JSX element representing an individual order placed
 */
const Order = ({ id, amount, amountShipping, items, timestamp, images }) => {
  return <div></div>;
};

export default Order;
